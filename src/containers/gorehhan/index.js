import React, { Component } from "react";
import List from "../../components/listresto/index";

export default class index extends Component {
  render() {
    return (
      <div>
        <List />
      </div>
    );
  }
}
